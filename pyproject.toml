[project]
name = "ml-sem"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[dependency-groups]
dev = [
    "fastapi>=0.136.1",
    "flask>=3.1.3",
    "pandas>=3.0.2",
    "pre-commit>=4.6.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
    "scikit-learn>=1.8.0",
    "uvicorn>=0.46.0",
]

[tool.uv]
workspaces = { members = ["apps/*", "libs/*"] }